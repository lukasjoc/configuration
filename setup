#!/bin/bash

# bash setup
cp -a .bashrc .bash_profile .profile $HOME

# vim setup
cp -a .vim .vimrc .gvimrc $HOME

# tmux and inputrc
cp -a .tmux.conf .inputrc $HOME

# git setup
cp -a .gitconfig .gitignore $HOME
